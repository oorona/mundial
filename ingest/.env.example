# Connection to the shared PostgreSQL on the dbnet network.
# The password is NOT here — it lives in secrets/db_password.txt (gitignored).
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_USER=mundial
POSTGRES_DB=mundial
