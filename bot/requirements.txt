cryptography>=42.0.0
discord.py==2.3.2
sqlalchemy==2.0.23
asyncpg==0.29.0
redis==5.0.1
pydantic>=2.9.0
pydantic-settings>=2.3.0
structlog==23.2.0
aiohttp==3.9.1
PyYAML>=6.0
# openai and anthropic commented out - large downloads, uncomment if needed
# openai>=1.10.0
# anthropic>=0.18.0
google-genai>=1.50.0
spacy>=3.7.0
# spaCy's CLI imports `click` directly (e.g. `from click import NoSuchOption` in
# spacy/cli/_util.py), but modern typer (0.26+) no longer depends on click, so it's
# no longer installed transitively — `python -m spacy download` fails at build time
# with "No module named 'click'". Declare click explicitly; current versions work fine.
click>=8.1.7
langdetect>=1.0.9
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-mock==3.12.0
