{
  "name": "fox-goal-poller",
  "version": "2.4.1",
  "private": true,
  "type": "module",
  "description": "Headless background poller: watches Fox X account(s) during game windows and uploads each video + its text to the Mundial server (no client-side classification). Immune to screen lock (no visible browser).",
  "scripts": {
    "login": "node login.mjs",
    "start": "node poll.mjs"
  },
  "dependencies": {
    "playwright": "^1.48.0"
  }
}
